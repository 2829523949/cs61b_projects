这是project3的设计文档，涵盖了工具的理解/任务阶段分层。

一.项目工具理解：

TERenderer：
控制图像渲染的工具
可以用initialize初始化给定高和宽的图像
可以把TEtile通过renderFrame方法传入来对图像进行修改

TEtile：
用来存储方块信息的数组
可以通过for循环的嵌套把Tileset信息传入数组中

Tileset:
方格的底层信息
分为NOTHING/WALL等类型
所有的方格最开始都应该储存NOTHING信息才可以图像化

注：lab09的BoringWorldDemo是这三个基本方法的运用实例，可以参考。

补充：可以给Random输入“种子”(这里为整数)来得到随机数(伪随机数)，相同的种子必定得到相同的随机数。
(反复用System.out.println(r.nextInt)来打印，同一个种子，打印的结果的顺序是相同的。）

二.project3a：

1.持久性设计：

由lab09的saveboard和loadboard为例，将图块信息描述符号化存入文档里面，再把文档的信息一行行读取出来生成图块。
持久性的工具库是FillUtils和StringBuilder。

2.世界生成算法：

需要满足以下要求：

（1）有效的：

世界必须是一个二维网格，使用我们的图块引擎绘制。图块引擎在 lab 9 中描述。
生成的世界必须包括不同的房间和走廊，尽管它也可能包括室外空间。
至少一些房间应该是矩形的，尽管您也可以支持其他形状。
你的世界生成器必须能够生成包含转弯的走廊（或者等效地，相交的直走廊）。随机世界应以适度的频率（20% 或更多的世界）生成转弯走廊。
不允许死胡同走廊。
房间和走廊必须有在视觉上与地板不同的墙壁。墙壁和地板在视觉上应与未使用的空间不同。
角落墙是可选的。
房间和走廊应该连接，即相邻的房间或走廊之间的地板上不应有间隙。
所有房间都应该是可到达的，即不应该有无法进入的房间。
房间不能剪切掉世界的边缘。换句话说，世界的边缘不应该有地板砖。
世界中不应存在过多的未利用空间。虽然这个标准比较主观，但请尽量保证房间和走廊能够占据世界面积的50%以上。

（2）足够的随机性：

世界必须是以伪随机方式生成的。伪随机性的概念在实验 9 中讨论过。
世界应该包含随机数量的房间和走廊。
房间和走廊的位置应该是随机的数量。
房间的宽度和高度应该是随机的尺寸。
走廊的宽度应为 1 个图块单位，长度随机。
每次生成的世界都应该显著不同，也就是说，不应出现具有容易预测特征的相同基本布局。

（3）根据seed来生成


函数块分布:AutograderBuddy部分处理基础输入的处理和世界生成的拼接。
Main部分处理后续的键盘交互与世界对应改变。
单独设一个interactive部分来编写互动代码

走廊生成：
在main函数里面接入graph，graph储存room信息，在world的generatewallays里面传入graph来进行辅助
连线的时候要注意是否有xy方向上的重叠，可以直接水平/竖直连线。